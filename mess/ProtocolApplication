(i hate my life)

goal:
    To make a protocl that sends bytes.

What i have:
    I have a struct with payload properites such as the std::string which is the main body, the PL_TYPE, the PL_LEN, and the PL_CTL.

What I want to do:
    Create a function that can serialize this packet so that it is ready to be sent via the socket.

What am i doing and how i plan on making it:
    SerializePacket function arranges the data so that it is ready for sending. It does not know what data there is. It just arranges it to our designed protocol and CONVERT it into big endian format so that its ready for sending into the wire.

    std::vector is a dynamic buffer. used unsigned int of 8 bits because i want each element to be of 1 byte. It is much simpler for sending binary data over the wire.

    i take input as the packet struct so that it knows what it is working on (by working with the variables provided by the struct), essentially knowin the blueprint of the arrangement. We start with the blank buffer and we then append the array with the contents of the packet as designed by our protocol. We also calculate the size of the payload, which is just a string, and we get the value by that. This LEN attribute will help our decoder to kno0w the size of our message string (or payload) We will be using big endiannes for this shit.

    Once the byte order is ready we then simply push it to the send() function to send it over the socket.






